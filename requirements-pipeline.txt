# Local pipeline dependencies. Not installed on Streamlit Cloud.
# Install with: pip install -r requirements-pipeline.txt
#
# If pip struggles with geopandas, use conda instead:
#   conda install -c conda-forge geopandas folium
#
# Add requests when a city needs an HTTP geocoder step (none does yet -
# downloads are done with curl and both built cities ship pre-geocoded).
pandas>=2.2
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
folium>=0.17

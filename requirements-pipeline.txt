# Local pipeline dependencies. Not installed on Streamlit Cloud.
# Install with: pip install -r requirements-pipeline.txt
#
# If pip struggles with geopandas, use conda instead:
#   conda install -c conda-forge geopandas folium
#
# requests: the Census bulk geocoder (pipeline/census_geocoder.py). Downloads
# themselves are done with curl.
# openpyxl: registers published only as XLSX (Sendai's permit lists, inside ZIPs).
# xlrd: the old .xls format (Kyoto's 2021-03-31 full permit list).
pandas>=2.2
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
folium>=0.17
requests>=2.32
openpyxl>=3.1
xlrd>=2.0

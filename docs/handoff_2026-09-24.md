# Handoff — 2026-09-24, cleanup session

**Everything below is on `master` as of the commit that adds this version.**
Nothing is uncommitted and nothing is waiting to be pushed.

---

## First

Run `git branch --show-current` and `git worktree list`. You must be in
`.claude/worktrees/cleanup` on `worktree-cleanup`; if not, stop and say so.
Never run `git worktree remove` on the folder this session was launched in.
Other sessions push often, so `git fetch` and `git merge --ff-only
origin/master` before starting.

## Role

The permanent cleanup session - the `consistency-sweep` skill. Prefer
shipping a check over a correction.

- Stage files by name; set the identity per command:
  `git -c user.name=dacekroberts -c user.email=49654908+dacekroberts@users.noreply.github.com commit ...`
- One `DECISIONS.md` entry per change, then `python scripts/decisions_index.py`.
  Resolve a conflict there with `python scripts/merge_append_only.py DECISIONS.md`.
- Fetch immediately before every push, with nothing slow in between.
- Published prose: draft it in chat and get the owner's approval before
  writing the file.
- Scratch output goes to the session scratchpad. No backslash or backtick in a
  Bash command: write a file and run it.

## Worktrees

| Folder | Owner | State |
|---|---|---|
| `cleanup` | this session | live; also holds ~10 GB of copied, gitignored caches (`data/denmark` alone is 5.5 GB), all copies from the main checkout or from `copenhagen`, so the drift check can re-render every city |
| `staging` | Staging Session | live |
| `amsterdam`, `rome`, `prague`, `sao-paulo` | build sessions | leave alone |
| `copenhagen` | Main Building Session | Copenhagen is live and the branch is fully merged, but its PLAN item still holds open owner actions. Retire it only on that session's word. |

Retired 2026-09-23 and 2026-09-24: `lille`, `gallant-brattain-00d045` (the
zoom-lag session), `oslo`. The only remote branch besides `master` is
`worktree-staging`.

## Retiring a worktree

`docs/session_roles.md`, steps 1-4. Each step was learned the hard way.

1. Confirm the branch is merged, the tree is clean, and the owning session is
   **not running**. Ask that session directly: the `cwd` that `list_sessions`
   reports does not show where a session is actually working.
2. List reparse points over the **whole** folder, and remove each junction on
   its own, non-recursively: `[System.IO.Directory]::Delete(path, $false)`.
   `data\france\raw` is a junction to the shared 3 GB SIRENE pair, and a
   recursive delete can follow it. This worktree has one too.
3. Copy any `data/<city>/` the main checkout lacks, then `git worktree remove`
   and `git branch -d` (never `-D`).
4. "Filename too long" comes from the worktree's `.venv-lean`: re-scan, then
   `cmd /c rmdir /s /q "\\?\<path>"`. An idle session that is still open
   locks its now-empty folder until the owner closes it.

## Shipped 2026-09-23 and 2026-09-24

Every check below was watched failing on a positive control before it was
trusted.

**Maps**
- **Phone-width line labels, the whole problem.** `LABEL_CLAMP_SCRIPT` slides a
  label inside the frame, then re-places any label that collides around its
  own line's tip. At 375 and 343: 68 clipped labels, 56 overlapping pairs and
  6 labels under a button or the legend became **2 overlaps** (Madrid's core
  at 343), with every map's view unchanged. `deploy-verify` (`map-chrome`)
  passed; it is live. Zooming out to fit was measured and rejected.
  **Optional follow-ups in `PLAN.md`:** a few re-placed labels sit ~54 px from
  their tip (Amsterdam's Metro 51), and labels can sit on cluster bubbles.

**Checks and reports** (all in `scripts/`)
- `check_map_labels.js` reads positions from the map object, runs at 375 and
  343, and refuses a label over the OSM credit.
- `check_render_current.py`: every map carries the current renderer's shared
  blocks, derived from `map_common.py`.
- `check_city_registry.py`: one `cities.py` entry per city page, and no dict
  with a repeated key - the Rome/Amsterdam merge fused two entries into one.
- `check_macro_labels.py` models the macro map's theme button, zoom group and
  credit.
- `check_stray_downloads.py`: browser-pane downloads land in the MAIN
  checkout's root. The `licence-read` agent runs it before reporting.
- `check_deploy_imports.py` no longer leaks clones into `%TEMP%`.
- `check_plan_done.py` (report only): open work outranks a citation, so an item
  with an unticked sub-item or "not yet" reads KEEP(open), never REMOVABLE.

**Words**
- "rail-transit" became "rapid-transit" site-wide (Toulouse's Téléo).
- "Commuter rail is excluded unless it runs like rapid transit", and four city
  pages say "most maps" (Copenhagen's S-tog after Dublin's DART).
- Milan's page: "one pin in seven". The stale-prose sweep of categories D, B and
  A made 3 corrections.
- PLAN.md trimmed from 1,695 to 1,494 lines.

## GitHub repository

- **Nothing to clean on GitHub itself.** The merged `dublin-build`,
  `milan-build` and `spain-app-wiring` were deleted on the owner's instruction.
- **The About panel is set** (description, Website link, 8 topics). This
  session never handles GitHub credentials, so any future change is the
  owner's to paste.
- **README:** cities listed by country, **no hand-kept total**. When a city
  lands, add its name under its country.
- **LICENSE keeps its scope section after the MIT text. Do not split it out.**
  That is why GitHub reports `NOASSERTION`. Edmonton's terms are recorded as
  satisfied BECAUSE LICENSE disclaims MIT over `outputs/`.

## Checks

All green at handoff: `check_provenance.py --strict` ·
`check_no_fetch_in_steps.py` · `check_scope_disclosure.py` (30 cities) ·
`check_theme_sync.py` · `check_macro_labels.py` · `check_render_current.py`
(30 maps) · `check_city_registry.py` (30 entries, 30 pages) ·
`decisions_index.py --check`.

`check_stray_downloads.py` is clean across all 8 checkouts. The last stray,
an MHLW notice (2020-07-10) on the food-permit system's terms, was filed on
the Staging Session's word as
`docs/licenses/mhlw-food-sanitation-system-terms-2020.pdf`. The Fukuoka and
Hiroshima briefs' MHLW open-data verdict rests on its 第3条.

Report-only: `check_stale_claims.py --only E` finds **20** universal claims
(re-read them when a city lands). `check_plan_done.py` finds 45 done items:
0 removable, 12 held by open work, the rest for a person to confirm.

## Open

- **MHLW's 2026-03-31 amendment** (`https://www.mhlw.go.jp/content/001683424.pdf`)
  to the stored permit-system terms is not archived. The README row warns to
  check it before treating the 2020 text as current. Archiving it means
  downloading a file, so it is the owner's call.
- **`copenhagen` worktree** - retire only on the Main Building Session's word.
- **When a city lands:** re-read the 20 universal claims, run
  `check_render_current.py` and `check_city_registry.py`, add it to the README
  under its country, and retire its worktree once its session says it is done.

## Parked by the owner - do not raise

- **Barcelona's council notification**: adjourned, because no working contact
  channel exists (`docs/gated_access.md` item 2).
- **Philadelphia's permission reply**: adjourned; a scheduled task fires on
  2026-09-28 at 09:00.
- **Marseille's front-page label touching Milan's dot**: left on the owner's
  call. Contact is reported, not failed, as with Philadelphia and New York.

## Rules that kept paying

- Keep a positive control, and check each negative case for its **expected
  message**, not just its exit code.
- Measure a claim in the rendered artifact, not in the file.
- Measure the BEFORE state first, then change one thing. The label placer's
  62-to-2 was only a finding because the baseline was taken first.
- Before a soft reset or squash, merge the newest master **first**. A reset
  onto a master that has moved silently reverts the commits you didn't have.
- Verify a peer's "facts" yourself before acting on them, and read a pointer
  search before changing anything others rely on (the LICENSE split was
  approved and then reversed because of one).
- When the usage limit is near, checkpoint in a local commit and hold the
  push. Never skip a gate to get something out.

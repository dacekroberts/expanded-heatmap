# Handoff — 2026-09-24, cleanup session

**Everything below is on `master` as of the commit that adds this file.**
Nothing is uncommitted and no cleanup branch is waiting.

---

## First

Run `git branch --show-current` and `git worktree list`. You must be in
`.claude/worktrees/cleanup` on `worktree-cleanup`; if not, stop and say so.
Never run `git worktree remove` on the folder this session was launched in.
Other sessions push often, so `git fetch` and `git merge --ff-only
origin/master` before starting.

## Role

The permanent cleanup session - the `consistency-sweep` skill. Prefer
shipping a check over a correction.

- Stage files by name; set the identity per command:
  `git -c user.name=dacekroberts -c user.email=49654908+dacekroberts@users.noreply.github.com commit ...`
- One `DECISIONS.md` entry per change, then `python scripts/decisions_index.py`.
  Resolve a conflict there with `python scripts/merge_append_only.py DECISIONS.md`.
- Fetch immediately before every push, with nothing slow in between.
- Scratch output goes to the session scratchpad. No backslash or backtick in a
  Bash command: write a file and run it.

## Worktrees

| Folder | Owner | State |
|---|---|---|
| `cleanup` | this session | live |
| `staging` | Staging Session | live |
| `copenhagen` | Main Building Session | **in progress - do not touch** |
| `prague` | none | an empty, unregistered folder, **kept on the owner's word**: Prague is viable again after staging's research, and a build may reuse it |

Retired 2026-09-23 and 2026-09-24: `lille`, `gallant-brattain-00d045` (the
zoom-lag session), `oslo`. The only remote branch besides `master` is
`worktree-staging`.

## Retiring a worktree

`docs/session_roles.md`, steps 1-4. Each step was learned the hard way.

1. Confirm the branch is merged, the tree is clean, and the owning session is
   **not running**. Ask that session directly: the `cwd` that `list_sessions`
   reports does not show where a session is actually working.
2. List reparse points over the **whole** folder, and remove each junction on
   its own, non-recursively: `[System.IO.Directory]::Delete(path, $false)`.
   `data\france\raw` is a junction to the shared 3 GB SIRENE pair, and a
   recursive delete can follow it.
3. Copy any `data/<city>/` the main checkout lacks, then `git worktree remove`
   and `git branch -d` (never `-D`).
4. "Filename too long" comes from the worktree's `.venv-lean`: re-scan, then
   `cmd /c rmdir /s /q "\\?\<path>"`. An idle session that is still open
   locks its now-empty folder until the owner closes it.

## Shipped this session

Everything below is on master, and every check was watched failing on a
positive control before it was trusted.

- **Phone-width line labels.** `LABEL_CLAMP_SCRIPT` slides a label that would
  be clipped just inside the frame: 68 clipped labels became 0, with the view
  unchanged. Zooming out to fit the labels was measured and rejected.
  `check_map_labels.js` now reads positions from the map object (a hidden
  pane leaves the DOM stale), runs at 375 and 343, and refuses a label over
  the OSM credit.
- **`check_render_current.py`**: every committed map carries the current
  renderer's shared blocks. The blocks are derived from `map_common.py`, and
  it catches both missing blocks and stale edits.
- **`check_macro_labels.py`** now models the macro map's theme button, zoom
  group and credit, and reproduces Oslo's hidden label to 0.1 px.
- **`check_deploy_imports.py`** no longer leaks clones into `%TEMP%` (54 down
  to 5; the last 5 age out on the next run).
- **`check_stray_downloads.py`**: browser-pane downloads land in the MAIN
  checkout's root. The `licence-read` agent now runs it before reporting.
- **`check_plan_done.py`** (report only). `PLAN.md` was trimmed from 1,695 to
  1,494 lines.
- **Site wording**: "rail-transit" became "rapid-transit" (owner's call, for
  Toulouse's Téléo). Milan's page now says "one pin in seven" (15.3%), and its
  step 2 prints that figure.
- **Stale-prose sweep** of categories D, B and A: 3 corrections; the rest were
  right as written.

## Checks

All green at handoff: `check_provenance.py --strict` ·
`check_no_fetch_in_steps.py` · `check_scope_disclosure.py` ·
`check_theme_sync.py` · `check_macro_labels.py` · `check_stray_downloads.py` ·
`check_render_current.py` · `decisions_index.py --check`.

Report-only: `check_stale_claims.py --only E` finds **24** universal claims
(re-read them when a city lands; Copenhagen is next). `check_plan_done.py`
finds **29** done items, each held for a stated reason.

## Open

- **Label collisions at phone width** (28 pairs in 7 cities), and labels under
  the legend or theme button (Barcelona, Edmonton, San Diego, Montréal at
  854). This is a `PLAN.md` item and app/chrome work, not cleanup's unless the
  owner routes it here.
- **When Copenhagen lands:** re-read the 24 universal claims, run
  `check_render_current.py`, then retire its worktree by the procedure above.
  Ask the Main Building Session first.

## Parked by the owner - do not raise

- **Barcelona's council notification**: adjourned, because no working contact
  channel exists (`docs/gated_access.md` item 2).
- **Philadelphia's permission reply**: adjourned; a scheduled task fires on
  2026-09-28 at 09:00.

## Rules that kept paying

- Keep a positive control, and check each negative case for its **expected
  message**, not just its exit code.
- Measure a claim in the rendered artifact, not in the file.
- Before a soft reset or squash, merge the newest master **first**. A reset
  onto a master that has moved silently reverts the commits you didn't have.
- Verify a peer's "facts" yourself before acting on them.

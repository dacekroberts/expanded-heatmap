# Handoff — 2026-09-24, cleanup session (evening refresh)

**Everything below is on `master` as of the commit that adds this version.**
Nothing is uncommitted, and no owner approval is pending.

---

## First

**The new session takes over THIS worktree; it does not get its own** (owner,
2026-09-24). Start it with its folder set to
`C:\Users\dacek\Documents\Portfolio\expanded-heatmap\.claude\worktrees\cleanup`,
not with the app's "new worktree" option. Close or archive the previous
cleanup session first: two sessions in one working tree overwrite each
other's uncommitted edits. This worktree also holds every city's copied
caches, which a fresh worktree would lack.

Run `git branch --show-current` and `git worktree list`. You must be in
`.claude/worktrees/cleanup` on `worktree-cleanup`; if not, stop and say so.
Never run `git worktree remove` on the folder this session was launched in.
Other sessions push often, so `git fetch` and `git merge --ff-only
origin/master` before starting.

## Priority: large maps go blank on iPhone (owner-approved renderer work, 2026-09-25)

The Main Building Session found and confirmed the cause on the owner's iPhone 16
Pro (iOS 18.7).
- **The cause**: WebKit, which every iOS browser uses, refuses to compile any
  single inline JS array literal above roughly 107k-131k elements. It is not
  memory or file size.
- **Where the line falls**: every failing map has a heat literal of 133,335+
  points (Mexico City, Taipei, São Paulo, Seoul). Every loading map is at
  106,652 or below, and Rio is within ~20% of the limit.
- **The full brief is in PLAN.md**, the "Seoul's map still 24 MB" item, from
  "CAUSE FOUND AND CONFIRMED... START HERE" onward (master `6648419`,
  `20af557`).
- **The hard threshold test comes before any cap:** bisect the exact limit on
  the phone for both shapes (heat triples, pin rows), in Safari as well as the
  Claude app, plus the JSON.parse ceiling. Record the device, iOS version,
  browser and counts in DECISIONS.
- **Then the fix, in `pipeline/map_common.py`**: emit large arrays as
  `JSON.parse('...')`, covering both heat layers and every `add_pin_layer` `var
  data`.
  - Change `scripts/check_personal_exposure.py` in the same commit, because it
    parses those arrays.
  - Apply it to every map, then re-render all, update the baselines, and run
    `check_render_current.py` and the browser test matrix.
  - Add a `check_*.py` that refuses an inline array literal above a cap set
    well under the measured limit.
  - Decide afterwards whether mobile mode is still needed.
- **This outranks the cleanup-sweep agent below:** the maps are broken for
  every iOS reader of the four largest cities.

### Downsides of the JSON.parse fix - handle them while building it

- **NaN blanks the whole map.** `json.dumps` emits NaN by default and
  JSON.parse rejects it. Use `allow_nan=False`, so a bad row fails the BUILD,
  not the published page.
- **Pin rows need escaping twice**: JSON inside a JS string inside a script
  tag. They carry names with quotes, apostrophes, backslashes, CJK, and
  possibly `</script>`.
  - Generate the string with library calls (`json.dumps` of the `json.dumps`
    output, plus `</` to `<\/`), never by hand.
  - Add a test with hostile names.
  - Heat data is numbers only.
- **Folium coupling.** The heat script comes from Folium's HeatMap template,
  so the fix overrides it, and a Folium upgrade could silently revert that.
  The planned oversized-literal check catches a revert.
- **Other readers of the HTML.** `check_personal_exposure.py` parses the pin
  `var data` arrays, so change it in the same commit. Grep `scripts/` for
  anything else that pattern-matches map HTML.
- **Minor:**
  - slightly larger files: escaped quotes on names add ~0.4 MB to Seoul's
    full map, and nothing to heat data;
  - data that is harder to grep;
  - one large diff when every map re-renders.
- **THE BIG ONE: it fixes the compile limit, not the weight.** Seoul's full
  map used ~758 MB of page memory in Chrome. On the iPhone a map may go from
  blank to slow, or to a killed tab. That is why the threshold test and the
  real-device browser matrix come before any reversal below.
- **Upside to record**: JSON.parse is usually faster than compiling an
  equivalent literal.
- **Rejected alternative**: chunking literals under the limit. It needs no
  escaping, but it depends on a device-specific number, so older phones stay
  at risk.

### Phone workarounds to REVERSE if JSON.parse works on real phones

All were made 2026-09-25 on the belief that the failure was memory or size,
which is now known to be wrong.
- **Reverse each only after** the threshold test and the browser matrix show
  the relevant FULL map loading on a real iPhone (and Android if one is
  available).
- **Each reversal gets a NEW DECISIONS entry** superseding the old one;
  DECISIONS is append-only.

1. **Seoul's whole-city heat layer** (`fb25ed7`).
   - **Now**: `all_city_heat=False` in `pipeline/seoul/step3_map.py`, plus the
     page-43 sentence the owner asked for: "Unlike most maps here, Seoul's has
     no whole-city heat layer...".
   - **To reverse**: set it True, remove or rewrite that sentence, re-render,
     and re-test on the phone.
   - **The one most likely to stay too heavy**: the map was ~30 MB with this
     layer.
2. **Mobile mode, four cities.**
   - **Seoul** (`6281d90`): the inline toggle and caption on
     `app/pages/43_Seoul_Heatmap.py`, `HEATMAP_LITE_HTML` in
     `pipeline/seoul/config.py`, and `render(output_path, pins)` in its step 3.
   - **Mexico City, São Paulo, Taipei** (`ee865ea`):
     `components.mobile_mode()`, called by pages 15, 31 and 46, and
     `lite_output_path=` in their step 3 files.
   - **Outputs**: `outputs/{seoul,mexico_city,sao_paulo,taipei}/heatmap_lite.html`.
   - **map_common's `pins=` and `lite_output_path=`** can stay as harmless
     options, or go.
   - **Removing the toggle changes `app/components.py`**, so the app needs a
     REBOOT after that push.
   - **Owner's framing**: keep mobile mode if the biggest full maps are slow
     even once they compile.
3. **Not a phone workaround, for comparison only.** Mexico City's missing
   whole-city layer predates the phone issue and was decided on size ("more
   than twice the points of the default one"). Check its DECISIONS entry
   before treating it as reversible.

### Which heat layer trips the limit, per city (Main Building Session, 2026-09-25)

`render_heatmap` adds the station-area layer (`in_rings`) first and the
whole-city layer second. Taipei's second layer equals its storefront total,
which confirms the order.

| City | Station-area layer | Whole-city layer | What trips the ~107k-131k limit |
|---|---|---|---|
| Seoul | 224,381 | dropped 2026-09-25 for the phone (`fb25ed7`) | the station-area layer alone |
| Mexico City | 133,362 | dropped 2026-09-22 for file size | the station-area layer alone |
| Taipei (Regional) | 103,587 (under) | 133,335 | ONLY the whole-city layer |
| São Paulo | 49,591 (under) | 219,578 | ONLY the whole-city layer |
| Paris (loads) | 84,125 | 87,164 | nothing |
| Rio (loads) | 30,269 | 106,652 | nothing, but the closest to the limit |

- **A whole-city layer is not a phone problem in itself**: Paris's and Rio's
  load on the owner's iPhone. Only a single literal over the limit fails.
- **Seoul's whole-city drop bought nothing on the phone.** It was made on the
  wrong model (memory or size). The map stayed blank because its station-area
  layer alone is over the limit. After JSON.parse, restoring that layer is a
  question of weight (load time, memory), not of compiling.
- **Taipei and São Paulo**: dropping the whole-city layer would fix the
  iPhone only by accident. **Do NOT take that route**: JSON.parse keeps the
  layer and fixes the compile.
- **Mexico City's 2026-09-22 drop** was a file-size decision made before any
  phone test. This neither supports nor undoes it; revisit it only on size.
- **When the fix lands**, a new DECISIONS entry should supersede `fb25ed7`'s
  reasoning explicitly: the whole-city drop did not address the cause.

### Mexico City's (and Guadalajara's) whole-city layer - reinstate after JSON.parse? (Main, 2026-09-25)

**Why it was dropped** (DECISIONS 2026-09-22, search "all_city_heat, off for
this city"):
- **It was a SIZE decision, made before any phone test.** The map was 25.7 MB,
  and the layer held 283,345 points. Dropping it took the map to 19.0 MB, and
  indexing the classification value later took it to 11.38 MB.
- **Transfer was never the problem**: 19 MB gzips to 3.07 MB. The reasons left
  were "parse cost and repository bytes".
- **It is recorded in** page 15 ("Unlike the other cities, this map has no
  whole-city layer ...") and in `excluded_categories.md`.
- **Guadalajara switched its layer off only to match Mexico City.** Its map
  is 3.4 MB (36,925 points).

**After JSON.parse: very likely yes, as an owner decision on measurements.**
- **The ceiling test must cover it**: the JSON.parse ceiling run needs to
  reach at least 283,345 points, above Seoul's proven 224,381. The planned
  500k does.
- **Parse cost**, one of the two remaining reasons, is what JSON.parse
  reduces.
- **Weight: about +6.7 MB in BOTH maps**, full (~11.4 to ~18 MB) and light
  (~3.9 to ~10.6 MB), because the light map keeps every heat layer. The data
  loads even while the layer is toggled off, and Leaflet.heat walks every
  point on each redraw while it is on. Measure load time and panning on the
  real iPhone with the layer off and on before recommending it.
- **Repository bytes do not change.** The owner decides whether ~6.7 MB per
  re-render is worth it.
- **If reinstated**:
  - `all_city_heat=True` in `pipeline/mexico_city/step3_map.py`;
  - rewrite the page-15 sentence (draft it for the owner first);
  - update `excluded_categories.md`;
  - add a new DECISIONS entry superseding 2026-09-22.
- **Guadalajara costs almost nothing**: ask the owner about it alongside
  Mexico City.

**Recommended order**:
1. the JSON.parse fix;
2. the threshold and browser tests on the current maps;
3. then Seoul's and Mexico City's whole-city layers restored ONE AT A TIME,
   each re-tested on the phone and decided by the owner on the measurements.

### Audit EVERY city for a dropped or trimmed whole-city layer (owner, via Main, 2026-09-25)

**Evidence so far, not a full audit:**
- **Only three maps have a single heat layer**, counting `L.heatLayer(` in
  each committed `heatmap.html`: Guadalajara, Mexico City and Seoul. Every
  other city has two, New York (44,360 / 62,441) and Los Angeles included.
- **The flag IS in the step files.** Main's grep for `all_city_heat` found
  nothing, but Cleanup found it on 2026-09-24:
  `pipeline/mexico_city/step3_map.py:93` and `pipeline/guadalajara/step3_map.py:71`
  (`all_city_heat=False`). So the glob missed; grep `pipeline/` recursively.

**To do:**
1. **Confirm the list from the rendered maps AND from DECISIONS and the
   configs.** A city may have trimmed its whole-city layer another way (a
   filtered subset, a cap, a sample) and still show two layers. Compare each
   whole-city layer's point count with the city's storefront total.
2. **For every city found**, record why the layer was dropped or trimmed
   (size, phone, or consistency; Guadalajara's was consistency with Mexico
   City), and whether that reason survives the JSON.parse fix.
3. **Give the owner ONE table**: city, points restored, MB added to the full
   and light maps, the original reason, and whether it survives. The owner
   decides each one. The order stays: JSON.parse, then the threshold and
   browser tests, then reinstatements one at a time, each re-tested on the
   phone.

### Investigation: "a search for all_city_heat found nothing" - RESOLVED 2026-09-25, a search artifact

On 2026-09-25 the Main Building Session's Grep of `pipeline/` with glob
`*/step*_map.py` for `all_city_heat` returned no matches, though three cities
use it.
- **Cleanup re-ran it as a plain recursive grep**
  (`grep -rn all_city_heat pipeline scripts --include=*.py`). It finds the
  flag passed directly, `all_city_heat=False`, in exactly three files:
  `pipeline/guadalajara/step3_map.py:71`, `pipeline/mexico_city/step3_map.py:95`
  and `pipeline/seoul/step3_map.py:49`.
- **So it is (a), not (b) or (c).** The code agrees with the committed maps:
  those three are the only single-heat-layer maps. There is no drift defect
  to clear before reinstatement work.
- **To do on Sunday, one line**: add to the `consistency-sweep` skill that a
  Grep with a `glob` like `*/step*_map.py` can silently miss files, so any
  audit that concludes "nothing uses X" is confirmed with a plain
  `grep -rn` (or the Grep tool with no glob) before it is trusted.

### Wrong on the live site now, whatever happens with the fix

- **The mobile-mode captions** (`components.mobile_mode`, and Seoul's own
  on page 43) say the dots are "more than a phone can hold in memory". The
  cause was never memory. **This is page prose, so draft the new wording for
  the owner first.**
- **On an iPhone, mobile mode helps nothing yet**, because the light maps
  also fail on their heat arrays.
- **PLAN.md's older sub-bullets** under the Seoul map-size item still frame
  it as memory and a ~9 MB threshold. The "CAUSE FOUND" bullet supersedes
  them: a consistency-sweep pass.

## Second, after the iPhone work: the cleanup-sweep agent (owner, 2026-09-24)

**Write `.claude/agents/cleanup-sweep.md`**, a scoped agent modelled on
`.claude/agents/deploy-verify.md`'s scope format, so recurring cleanup runs in
the agent's own fresh context and returns a short report instead of costing
many steps here. Scopes:

- `city-landed`: the new city's rows in `docs/map_inconsistencies.md`
  (all four tables and any theme it changes), re-read the universal claims
  (`check_stale_claims.py --only E`), run `check_render_current.py`,
  `check_city_registry.py`, `check_inconsistency_list.py`, and add it to the
  README under its country.
- `stale-claims`: run `check_stale_claims.py`, verify each flagged claim
  against the source, and return DRAFT wording. It never writes published prose.
- `retire-worktrees`: `docs/session_roles.md` steps 1-4, including
  copying data the main checkout lacks and unlinking junctions alone after a
  whole-worktree reparse scan. It never removes a worktree whose session has
  not confirmed it is done.
- `plan-trim`: sort `check_plan_done.py` candidates into removable and keep,
  with reasons.

Owner approvals and published wording still come back to the owner. Why:
tonight's worktree retirement took about 15 steps in a 580k-token session. As
an agent it is one call plus the approvals.

**Two working habits, same reason (owner, 2026-09-24):**

- **Start each cleanup batch in a FRESH session from this handoff**, rather
  than continuing a long one.
  - **Why**: the cost of every step grows with everything the session has
    already done. By the end of 2026-09-24 this session held three days of
    work, and a one-line edit cost roughly what a small city build did at its
    start.
  - **What changes**: "the permanent cleanup session" is a permanent cleanup
    ROLE. The handoff, DECISIONS.md and `docs/map_inconsistencies.md` carry
    what matters between batches.
- **Keep big text out of the conversation.** Some of that session's cost was
  avoidable:
  - **Whole files pasted in**, including automatic "file changed on disk"
    notices that dumped large parts of `app/cities.py` and `app/components.py`.
  - **Long diffs and full check output.**

  So print only the tail of a command's output (`| tail -3`), read a file by
  line range rather than whole, let an agent do broad reading and return a
  summary, and never `cat` a large generated file (a `heatmap.html`, a
  businesses CSV) into the transcript.

## Then, if the week's budget allows: an honest efficiency review (owner, 2026-09-24)

The owner asked for a review of the WHOLE project's least efficient parts,
**including the owner's own habits**, with honest findings rather than
reassurance. It is expensive, so only run it if it can be meaningful. Keep it
to the process: how work gets done, verified, approved and shipped. It is not
a code review.

- **Run it as ONE agent in its own context** (general-purpose, or the new
  `cleanup-sweep` with a `review` scope), at the start of a week. Its report
  comes back as a ranked list: each finding with its evidence, the estimated
  saving, and whose habit it is (the owner's, a session's, or the project
  structure's).
- **Evidence, not impressions.** Git history (`git log --since`, merge counts,
  files rewritten per day), the size of what every session loads, DECISIONS'
  index (never the whole file), session usage (`get_usage`), `check_*` run
  counts, and how many approval round-trips a change needed.
- **Starting figures, measured 2026-09-24. Verify them; don't trust them:**
  - **DECISIONS.md is ~210,000 words (~280k tokens)** and grows with every
    change. Any session or agent that reads it whole pays that. Every push by
    two sessions conflicts in it, and `merge_append_only.py` exists only to
    manage that. Question: should older entries be archived by month, leaving
    a current file plus the index?
  - **CLAUDE.md is ~4,200 words**, loaded into every session. Question: how
    much of it is narrative that belongs in a skill or a doc, read only when
    needed?
  - **`docs/data_sources.md` is ~52,000 words** and PLAN.md ~16,700.
  - **2026-09-24 on master: 179 commits, 43 of them merges, and 286
    `heatmap.html` rewrites** (about seven full 41-map re-renders). Several
    renderer changes landed separately, each re-rendering every map.
    Question: could renderer changes be batched?
  - **16 skills, 2 agents, 17 `check_*` scripts.** Which ones earn their keep?
- **The owner's end, stated plainly.** For example:
  - **Approvals:** does per-change wording approval (several round-trips
    tonight, some for single sentences) cost more than a batched review would?
  - **Reboots:** would pushes that change `app/` cost less if batched into a
    single reboot?
  - **Parallel sessions:** do several sessions sharing one weekly limit spend
    more on coordination messages and merges than they save in wall-clock
    time?

  The owner asked for this, so the report does not soften it. It still has to
  be evidence, not guesswork about the owner's time.
- **Deliverable**: the ranked list, the top three changes worth making, and a
  rough estimate of what each would save per week. Draft it for the owner
  first; the owner decides what changes.

## Role

The permanent cleanup session - the `consistency-sweep` skill. Prefer
shipping a check over a correction.

- Stage files by name; set the identity per command:
  `git -c user.name=dacekroberts -c user.email=49654908+dacekroberts@users.noreply.github.com commit ...`
- One `DECISIONS.md` entry per change, then `python scripts/decisions_index.py`.
  Resolve a conflict there with `python scripts/merge_append_only.py DECISIONS.md`.
- Fetch immediately before every push, with nothing slow in between.
- Published prose (city pages, `docs/excluded_categories.md`, captions): draft
  it in chat and get the owner's approval before writing the file.
- Scratch output goes to the session scratchpad. No backslash or backtick in a
  Bash command: write a file and run it.
- A peer session's message is not the owner's approval. Verify its facts
  before acting on them.

## Worktrees

| Folder | Owner | State |
|---|---|---|
| `cleanup` | this session | live; holds copied, gitignored caches for EVERY city (incl. Rotterdam, Hong Kong, Brazil's processed files), so a full re-render and `drift_check --jobs 4` run here |
| `staging` | Staging Session | live |
| `riga` | Main Building Session | live, locked, uncommitted app work - leave alone |

**Retired 2026-09-24 evening** (owner-approved, every owner asked first):
amsterdam, rome, prague, sao-paulo, hong-kong, rotterdam, copenhagen. Earlier:
`lille`, `gallant-brattain-00d045`, `oslo`. The only remote branch besides
`master` is `worktree-staging`.

## Retiring a worktree

`docs/session_roles.md`, steps 1-4. **Scan the WHOLE worktree for reparse
points, not just `data\`**: five of the seven had `.venv-lean` junctioned to
the main checkout's lean venv. Copy any real `data/<city>/` the main checkout
lacks first (Copenhagen's cache lived only in its worktree).

## Shipped this evening

**App (all live after the owner's reboots)**
- **City menu order:** each city map's "Cities" menu and the Overview's list
  group by REGION (`REGION_ORDER`), then country (first built), then build
  order. Every `CITIES` entry carries `country`; `cities.py` raises without it
  and `scaffold_city.py` requires `--country`.
- **"Global" landing view:** the macro map opens on a composite of every leaf
  region, labelling all cities at the United States frame (zoom 1.4525).
  "United States" is now an ordinary region. LA's label dy 26 -> 24 cleared
  Guadalajara. `check_macro_labels.py` and `check_deploy_imports.py` score
  Global only on the cities whose markers fall on the 343 px phone canvas.
- **"Global View"** replaced "All cities" on every city map's button (Main's
  render). The HIDDEN nav link keeps the text "All cities" - old maps' JS
  finds it by that text.
- **Front-page OSM credit:** painted above the city dots (deck.gl had stacked
  it at z-index -1), legible in light mode (was 1.18:1), opaque in both
  themes, OSM link to /copyright. `node scripts/check_macro_attribution.mjs
  <base> 375,768,1200` measures all of it in both themes; live: `<app>/~/+`.
- **Nine stale "only city" claims corrected** (owner-approved wording): Miami,
  San Francisco, Mexico City, Dublin, Paris, New York pages and five passages
  of `excluded_categories.md`. Principle: drop the comparison rather than
  update a count.

**Maps (all 41 re-rendered twice, identical apart from the intended change)**
- **Dark-mode line labels** start from their line's own colour, lightened only
  to 4.5:1, then separated per city (`linecolour.dark_label_colours`). 21
  collapsed pairs and orange-to-yellow labels gone. `check_map_markup.py` part
  C refuses two different lines sharing a dark label colour.
- **CJK fonts per map language:** `theme.font_stack(lang)`,
  `render_heatmap(lang=...)` sets `<html lang>` and `--hm-font`; shared blocks
  read `var(--hm-font, FONT_STACK)`. Hong Kong is `zh-HK` (Microsoft JhengHei
  now draws every Han glyph; Yu Gothic drew 3 of 5 before).
  **`render_heatmap` raises on CJK business names with no `lang`** - Taiwan
  (`zh-TW`), Seoul (`ko`) and Japan (`ja`) will meet it. `cjk-text` skill
  updated.

**The cross-city inconsistency list** - `docs/map_inconsistencies.md`
- 12 reader-facing themes, four city-by-city tables, open questions. The owner
  will place it on the site **after the last viable build** (PLAN item, and a
  memory reminder). Keep it current: `python
  scripts/check_inconsistency_list.py` fails when a city lacks a row in any
  table; wired into publish-city step 4 and add-city.

**Housekeeping**
- PLAN.md 1,913 -> 1,738 lines. The Riga PDF was moved (not deleted) to the
  Main Building Session's licence scratch folder.

## Checks

All green at handoff: `check_provenance.py --strict` ·
`check_no_fetch_in_steps.py` · `check_scope_disclosure.py` (41) ·
`check_theme_sync.py` · `check_macro_labels.py` (10 regions) ·
`check_render_current.py` (41) · `check_city_registry.py` (41) ·
`check_map_markup.py` (A-C) · `check_inconsistency_list.py` (41) ·
`check_stray_downloads.py` (4 checkouts) · `decisions_index.py --check` ·
`check_macro_attribution.mjs` (live).

Report-only: `check_stale_claims.py --only E` finds **18** universal claims
(re-read them when a city lands). `check_plan_done.py` finds 47 done items:
0 removable, 23 for a person to confirm, 11 held by open work.

## Open

- *(Done: the Toronto legend passage and the French address-only sentence,
  both approved and live after the next reboot. The French shares are rounded
  from measured figures - Paris 62.7%, Marseille 56.2%, Lille 50.6%, Toulouse
  48.1%, Rennes 40.9%; re-measure from `name_is_address` if a French city is
  rebuilt.)*
- **Toronto's retail share** (inconsistency list Q18): the baseline's buckets
  (870 of 19,575) and the cleaned rows the map draws (814 of 19,384) are both
  real. Settle by reading Toronto's step 2, not by editing a number.
- **Optional label polish** (PLAN): a few re-placed labels sit ~54 px from
  their tip; labels can sit on cluster bubbles.
- **When a city lands:** add its rows to `docs/map_inconsistencies.md`,
  re-read the 18 universal claims, run `check_render_current.py`,
  `check_city_registry.py` and `check_inconsistency_list.py`, add it to the
  README under its country, and retire its worktree once its session says so.

## Parked by the owner - do not raise

- **Barcelona's council notification**: adjourned (no working contact channel).
- **Philadelphia's permission reply**: adjourned; a scheduled task fires on
  2026-09-28 at 09:00.
- **Marseille's front-page label touching Milan's dot**: left on the owner's
  call.

## Rules that kept paying

- Keep a positive control, and check each negative case for its **expected
  message**, not just its exit code. Tonight's credit check failed 13 ways on
  the live site before the fix and 0 after.
- Measure a claim in the rendered artifact, not in the file: which font drew a
  glyph (CDP `CSS.getPlatformFontsForNode`), what is painted above the credit
  (the full `elementsFromPoint` stack - `elementFromPoint` skips
  `pointer-events: none`).
- After a re-render, diff old vs new with folium's random ids masked and the
  intended change undone. Anything left is unintended.
- A Streamlit server caches imported modules. Restart it before re-measuring
  an `app/` change; the owner's reboot is the same fact on the live site.
- A count in prose is a bet on the next city. Name the category or the members
  instead; the spacing-filter sentence was "five cities" for an hour.
- Before a soft reset or squash, merge the newest master **first**.
- When the usage limit is near, checkpoint in a local commit and hold the
  push. Never skip a gate to get something out.

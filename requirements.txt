# Streamlit Cloud installs from this file.
# Keep it lean: no geopandas, no GDAL, no compiled geo stack - the geo stack
# can't be a runtime dependency on Streamlit Cloud. The heavy dependencies
# live in requirements-pipeline.txt and are only needed locally, to
# regenerate outputs/. The app only reads pre-built files from outputs/.
# Add charting libraries here only when the app actually draws charts.
# streamlit>=1.64: the macro map uses st.pydeck_chart click selection with its
# default full width (older releases need the now-deprecated use_container_width).
# pydeck itself ships with Streamlit, so it needs no line of its own.
# <2: a guard against a major release changing the APIs the app relies on
# (st.iframe, st.pydeck_chart selection, st.container(horizontal=True)).
streamlit>=1.64,<2
pandas>=2.2
